import styled from 'styled-components'
import Typography_ from '@material-ui/core/Box'
import theme from 'theme'

export const Typography = styled(Typography_)`
  color: 'grey';
  font-weight: bold;
  margin-right: ${theme.spacing(1)}px;
`
